# Config

This directory holds all configuration-related files for the application.

-   `db.js`: Database connection setup (MongoDB).
-   `env.js`: Environment variable loading and validation using Zod. Ensures that all required environment variables are present and correctly formatted at application startup.